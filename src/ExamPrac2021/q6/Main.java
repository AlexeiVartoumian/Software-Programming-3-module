package ExamPrac2021.q6;

public class Main {
    public static void main(String[] args) {

        /*
        import org.jetbrains.annotations.NotNull;
        import java.util.List;
        public class MysteryScript {
               public static Object mystery(@NotNull List list) {
                    if (list.equals(List.of())) return "empty";
                    if (list.size() == 1) return list.get(0);
                return mystery(list.subList(1, list.size()));
                }
            }
         */

        /*
        Above uses annotations to error handle empty lists and force the method
         to only proess non null created lists and process them to some criteria
         if empty return a string , if size one return el other wise return all the elements as a list After the first one
         */
    }
}
