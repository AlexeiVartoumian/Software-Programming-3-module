package examIV.Q10DI;

public class Main {
    public static void main(String[] args) {

        /*
        The reflection API allows an executing Java program to examine or "introspect" upon itself.

   Complete the following method (using reflection) to list which methods are defined for a class passed as an argument to the method.
   You should include in your output the return type (if any) of each method.

public static void reflect(String cl){
  try {
    Class cls = Class.forName(cl);
    // YOUR CODE GOES HERE
  }
  catch (Exception ex) {
    System.err.println(e);
  }
}
         */
    }
}
